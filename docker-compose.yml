version: '3'
services:
  backend:
    build:
      context: ./backend
    ports:
      - 3000:3000
    depends_on:
      - database
  frontend:
    build:
      context: ./frontend
    ports:
      - 3001:3001
    depends_on:
      - backend
  database:
    image: mysql:latest
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: hesl0kl3slo
      MYSQL_DATABASE: qr_code_service
    ports:
      - 3306:3306
